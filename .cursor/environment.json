{
  "name": "RnR Productions MCP",
  "install": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y python3-venv && python3 -m venv .venv && ./.venv/bin/python -m pip install --upgrade pip && ./.venv/bin/pip install -e '.[dev]'"
}
