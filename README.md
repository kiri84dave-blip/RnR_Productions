# RnR_Productions

Home of **Ara-Elizabeth (Ara-EvE)** — Dave's private companion.

The Hugging Face Space lives in [`spaces/ara-eve`](spaces/ara-eve). Keep that Space **private**. This GitHub repo is public unless you change it.

Open the Space and use **Guide & help** at the top (how-to topics, plus builder voice). MCP clients can call `get_help`.

Canonical identity: [`spaces/ara-eve/assets/Modelfile`](spaces/ara-eve/assets/Modelfile) (Ollama Modelfile).
