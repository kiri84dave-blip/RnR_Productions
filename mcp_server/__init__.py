"""RnR Productions MCP "server feeder".

A Model Context Protocol server that exposes a controlled set of tools an agent
can use to inspect and operate the machine it runs on (and, in the future,
remote server machines it is pointed at).
"""

__version__ = "0.1.0"
